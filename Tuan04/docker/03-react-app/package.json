{
  "name": "react-docker-app",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "node build.js",
    "start": "npx serve -s dist -l 3000"
  },
  "devDependencies": {
    "serve": "14.2.4"
  }
}
